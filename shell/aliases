# ALIASES
# -------

# Directory listings
alias ls="ls -G"
alias ll="ls -lh"
alias la="ls -lah"
alias lt="ls -laht"
alias lh="ls -lad .*"

# DOS commands
alias cls="clear"
alias dir="ls"

# Dev environment setup
alias devno=". ~/.dev/dev_no"
alias devpy=". ~/.dev/dev_py"
alias devrb=". ~/.dev/dev_rb"

# Web servers
alias nws="devno && http-server ./ &; open -g http://localhost:8080"
alias pws="python -m SimpleHTTPServer &; open -g http://localhost:8000"

# Homebrew
alias bw="brew"
alias bwu="brew upgrade && brew cleanup"
alias bk="brew cask"
alias bko="brew cask outdated"

# Git
alias git="hub"
alias gt="git"

# Jupyter notebook
alias jpn="devpy && pyenv shell jupyter && jupyter notebook"

# Directory shortcuts
alias ddd="cd /Users/Darren/Documents/Development"
